# Connectors Package

One adapter per explicitly activated provider/source family. Every connector includes bounded
retrieval, timestamps, schema fingerprints, contract tests, freshness logic and evidence metadata.
Activation is an operator decision, not a legal-permission conclusion.
