import { schemas } from "../schemas.js";

export const qualityToolSchemas = {
  monitor_targets_list: schemas.monitor_targets_list,
  monitor_target_get: schemas.monitor_target_get,
  incidents_list: schemas.incidents_list,
  incident_get: schemas.incident_get,
  status_summary: schemas.status_summary,
};
