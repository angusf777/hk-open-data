import { schemas } from "../schemas.js";

export const sourceToolSchemas = {
  sources_list: schemas.sources_list,
  source_get: schemas.source_get,
  source_records_query: schemas.source_records_query,
  source_record_get: schemas.source_record_get,
};
